# Build artifacts
spike/spike
spike/inspect.sh
spike/inspect2.sh
# Anything in tests/ that isn't a .nim source is a build artifact.
tests/*
!tests/*.nim
tests/*.so
tests/*.dylib
tests/*.dll
nimcache/
*.o
*.exe

# milpa-managed dep symlinks + nim.cfg (regenerated on `milpa fetch`).
# milpa.kdl and milpa.lock ARE committed.
_deps/
nim.cfg
